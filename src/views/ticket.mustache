<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
        body{
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .ticketContainer{
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
            width: 450px;
        }
        .ticketContainer h2{
            margin-top: 0;
            margin-bottom: 20px;
            text-align: center;
            color:rgb(90, 90, 90)
        }
        .ticketContainer li{
            color:rgb(90, 90, 90);
            text-align: left;
            padding-block: 10px;
            font-size: 22px
        }
                .ticketContainer button {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
        }
        .ticketContainer button:hover {
            background-color: #0056b3;
        }
</style>
<script>
function handlerClickComprados(){
    console.log("entrou")
    window.location.href = `/page/bought`
}

</script>
<body>
    <div class='ticketContainer'>
    <h2>{{name}}<h2>
    <ul>
        <li>Catetogia: {{category}}</li>
        <li>Valor: {{price}}</li>
        <li>Local: {{place}}</li>
    </ul>
    <button onclick="handlerClickComprados()" >Abrir outro ingresso</button>
    </div>
</body>
</body>
</html>